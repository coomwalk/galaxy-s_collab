set(target_sample "sample_for_all")

include_directories(${target_sample} PUBLIC
    "${CMAKE_CURRENT_SOURCE_DIR}/../include/include_for_arithmetic"
    "${CMAKE_CURRENT_SOURCE_DIR}/../include/include_for_polinom"
)

add_executable(${target_sample} 
    "main_arithmetic.cpp"
    "main_polinom.cpp"
)

target_link_libraries(${target_sample}
    src_lib_for_arithmetic
    src_lib_for_polinoms
)